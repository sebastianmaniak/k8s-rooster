---
# Nginx reverse proxy settings

# Explicit backend target (Flask/Gunicorn)
# Set this to the current DHCP-assigned IP of compliance-app (VMID 201)
compliance_backend_host: 172.16.10.187
compliance_backend_port: 5000
