---
# Webserver variables

nginx_server_name: "_"

# Reverse proxy destination (Flask/Gunicorn)
app_host: "{{ compliance_app_ip }}"
# Gunicorn listens on 8000 (see compliance_portal systemd unit)
app_port: 8000

# Optional hardening / headers
nginx_client_max_body_size: "10m"
