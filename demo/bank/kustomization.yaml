apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
# Demo workloads
- namespaces.yaml
- crashloop-pod.yaml
- istio-virtualservice.yaml
- stateless-workloads.yaml
- resource-quotas.yaml
# kagent agent + tools
- bank-platform-agent.yaml
- prometheus-mcp-server.yaml
- prometheus-mcp-remote.yaml
# Prometheus stack is an ArgoCD Application — apply separately
# - prometheus-stack.yaml

commonLabels:
  demo: bank
  managed-by: kagent-demo
