# OpenAI backend dedicated to bank agents
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayBackend
metadata:
  name: bank-openai
  namespace: agentgateway-system
spec:
  ai:
    provider:
      openai:
        model: gpt-4.1
  policies:
    auth:
      secretRef:
        name: openai-secret
