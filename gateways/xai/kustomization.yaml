apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - backend.yaml
  - route.yaml
  - xaigateway.yaml
  - rate-limit-config.yaml

commonLabels:
  app.kubernetes.io/name: xai-gateway
  app.kubernetes.io/component: agentgateway

namespace: agentgateway-system
