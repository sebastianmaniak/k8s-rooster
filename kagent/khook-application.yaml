apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: khook
  namespace: argocd
spec:
  destination:
    namespace: kagent
    server: https://kubernetes.default.svc
  project: default
  source:
    repoURL: https://github.com/kagent-dev/khook.git
    targetRevision: main
    path: helm/khook
    helm:
      valuesObject:
        kagent:
          # Route through auth proxy instead of directly to kagent-controller
          # The proxy injects OIDC tokens before forwarding to the controller
          apiUrl: http://khook-auth-proxy.kagent.svc.cluster.local:8083
          userId: admin@kagent.dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
