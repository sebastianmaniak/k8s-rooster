apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: bank-enterprise-policies
  namespace: argocd
spec:
  destination:
    namespace: agentgateway-system
    server: https://kubernetes.default.svc
  project: default
  source:
    repoURL: https://github.com/ProfessorSeb/bank-agent.git
    targetRevision: main
    path: k8s/enterprise-policies
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
