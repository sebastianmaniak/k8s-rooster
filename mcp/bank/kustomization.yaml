apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - backend.yaml
  - route.yaml
  - policy.yaml

commonLabels:
  app.kubernetes.io/name: bank-mcp
  app.kubernetes.io/component: agentgateway-mcp

namespace: agentgateway-system
