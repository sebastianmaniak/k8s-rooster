apiVersion: v1
kind: Service
metadata:
  name: bank-mcp-server
  namespace: agentgateway-system
  labels:
    app: bank-mcp-server
spec:
  selector:
    app: bank-mcp-server
  ports:
  - protocol: TCP
    port: 3001
    targetPort: 3001
    appProtocol: kgateway.dev/mcp
  type: ClusterIP
