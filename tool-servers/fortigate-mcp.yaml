apiVersion: kagent.dev/v1alpha2
kind: RemoteMCPServer
metadata:
  name: fortigate-mcp
  namespace: kagent
spec:
  description: FortiGate FortiOS REST API MCP server
  url: http://fortigate-mcp-server.kagent:8080/mcp
  protocol: STREAMABLE_HTTP
  timeout: 10s
  terminateOnClose: true
