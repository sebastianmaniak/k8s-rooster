apiVersion: kagent.dev/v1alpha2
kind: RemoteMCPServer
metadata:
  name: vault-mcp
  namespace: kagent
spec:
  description: HashiCorp Vault KV and PKI management MCP server
  url: http://vault-mcp-server.kagent:8080/mcp
  protocol: STREAMABLE_HTTP
  timeout: 30s
  terminateOnClose: true
